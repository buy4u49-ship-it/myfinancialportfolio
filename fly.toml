app = "app-wandering-night-1272"
primary_region = "nrt"
kill_signal = "SIGINT"
kill_timeout = "5s"

[build]
  dockerfile = "Dockerfile"

[processes]
  app = "python market_price_worker.py"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
